services:
  - type: web
    name: martech-integration-builder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: KLAVIYO_PRIVATE_API_KEY
        sync: false
